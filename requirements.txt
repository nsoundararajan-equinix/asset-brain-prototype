# Asset Brain Production Requirements - Core Dependencies Only

# Google Cloud Dependencies
google-cloud-spanner>=3.40.0
google-cloud-logging>=3.5.0
google-auth>=2.20.0

# Core Dependencies  
flask>=3.0.0
gunicorn>=21.0.0
python-dotenv>=1.0.0

# Data Processing (Python 3.13 compatible)
# Note: Remove pandas/numpy for now to avoid build issues